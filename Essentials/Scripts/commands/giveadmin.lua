-- Gives admin to another user with them typing /adminpassword <password>
local giveadmin = {
    adminOnly = true,
    run = function(sender, rawMessage, commandArgs)
        print("Not Implemented Yet...")
    end
}

return giveadmin