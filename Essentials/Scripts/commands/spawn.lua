-- Spawn the specified Pal/NPC
local commandContext = {
    adminOnly = true,
    run = function(sender, rawMessage, commandArgs)
        print("Not Implemented Yet...")
    end
}

return commandContext